

.sign-up-page
  .sign-up-page__contents
    %h2.sign-up-page__contents__title 新規会員登録
    = form_for(resource, as: resource_name, url: registration_path(resource_name), class: "sign-up-page__contents__form") do |f|
      = render "devise/shared/error_messages", resource: resource
      .field
        .field-label
          = f.label :username, "ユーザー名"
        .field-input
          = f.text_field :username, autofocus: true, class: "field-input__form"

      .field
        .field-label
          = f.label :birth_date, "生年月日"
        .field-input
          = f.date_select :birth_date, use_month_numbers: true,start_year: 1930, end_year: (Time.now.year - 10), default: Date.new(1989, 1, 1), class: "field-input__form"

      .field
        .field-label
          = f.label :email, "email"
        .field-input
          = f.email_field :email, autofocus: true, class: "field-input__form"

      .field
        .field-label
          = f.label :password, "パスワード"
          - if @minimum_password_length
            %em
              (#{@minimum_password_length}文字以上)
        .field-input
          = f.password_field :password, autocomplete: "new-password", class: "field-input__form"

      .field
        .field-label
          = f.label :password_confirmation, "パスワード(確認用)"
        .field-input
          = f.password_field :password_confirmation, autocomplete: "new-password", class: "field-input__form"

      .actions
        = f.submit "登録する", class: "submit-btn"

      = render "devise/shared/links"